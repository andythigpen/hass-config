firetv_recents:
  alias: FireTV Recents
  sequence:
    - service: script.firetv_home
      alias: "call firetv_home"
    - wait_template: "{{ states.script.firetv_home.state == 'off' }}"
    - service: hdmi_cec.send_command
      alias: "hdmi command down"
      data:
        raw: "F8:44:02" # down
    - service: hdmi_cec.send_command
      alias: "hdmi command down"
      data:
        raw: "F8:44:02" # down
    - service: hdmi_cec.send_command
      alias: "hdmi command down"
      data:
        raw: "F8:44:02" # down
    - delay:
        milliseconds: 1
